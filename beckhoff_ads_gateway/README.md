# Beckhoff ADS Gateway v0.3.2

Diagnostic release that tests two ADS endpoints before reading the symbol:

- TwinCAT system service port `10000`
- TwinCAT 2 PLC Runtime 1 port `801`

It also logs the actual local AMS address used by pyads.
