# Beckhoff ADS Gateway v0.3.3

Symbol-browser diagnostic release.

New functionality:

- requests all PLC symbols published through ADS
- optionally filters names, types and comments
- writes symbol names, data types and ADS addresses to the app log
- retains the v0.3.2 connection diagnostics
