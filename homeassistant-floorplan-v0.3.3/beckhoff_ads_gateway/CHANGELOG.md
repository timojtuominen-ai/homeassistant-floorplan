# Changelog

## 0.3.3

- Added PLC ADS symbol browser using `Connection.get_all_symbols()`.
- Added `list_symbols`, `symbol_filter` and `max_symbols` options.
- Logs symbol name, type, index group, index offset and comment.
- Retained system-service and PLC-runtime diagnostics from v0.3.2.
