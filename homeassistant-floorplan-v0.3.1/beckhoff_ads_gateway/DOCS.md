# v0.3.1 test

This version opens the ADS connection and immediately reads:

`.arrHVACStates[4].lrActualValue`

It intentionally does not call `read_state()` first.

After installing or updating, start the app and copy its full log.
